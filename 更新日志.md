## v1.44.0（开源脱敏版 · 2026-09-26）

主人两条反馈：①「演示内容可以少一些，没必要显示所有设备」；②「模拟签名也加上」。

### 一、内容精简（38 台 / 10 房间，观感不再冗长）

| 维度 | 改前（v1.43.0） | **改后** |
|---|---|---|
| 设备 | 124 | **38** |
| 房间 | 15 | **10** |
| 签名人 | 8 | **4**（演示员甲~丁） |
| 科室 | 力学/金相/化学/机加 | **力学/金相/化学**（机加无房间，去掉） |
| 点检记录 | 3843 条（40 天） | **619 条（20 天）** |
| 数据体积 | 约 1.06MB | 约 3.7MB（含签名图） |

保留了该有的观感：
- **持久蠕变组**（蠕变试验室 1 + 2，15+6=21 台）仍是密集房；
- 覆盖 **力学 / 金相 / 化学** 三个科室 + 全部设备品类（蠕变/冲击/拉伸/疲劳弯曲/硬度/落锤/金相/化学分析/危化品洗眼器）；
- 显微镜（MET-INV 金相 ×2 + MET-UP 体视 ×1）仍**无模板、只走「显微镜维护记录」专项**，与工厂一致。

### 二、模拟签名（程序生成的 SVG 手写签名图）

工厂的 `signature_image` 是**真人手写签名 PNG（base64 照片）**，属于 PII，绝不能进演示站。
这里用**程序生成的 SVG 手写签名**替代：

- 每个演示员一张「演示员X」签名图，**横版（横排）+ 竖版（竖排）**两版；
- 用楷体 + 每字轻微旋转/位移模拟手写感，透明背景，`data:image/svg+xml;base64` 内嵌；
- 挂到三个真实消费点：大屏卡片、大屏「最近点检记录」签名列、点检表签名格、温湿度记录员栏
  —— 与真实签名走**同一条渲染路径**（前端 `<img src>` 直接引用）。

**体积与编码优化**：
- SVG 用 `base64` 而非 `urlencode`（中文 UTF-8 下 urlencode 放大 3 倍，base64 只 1.33 倍）；
- 精简 SVG（去掉 viewBox、缩短字体族列表），单张签名图压到 ~850 字符；
- 点检记录从 40 天减到 20 天，控制内嵌签名图带来的体积。

**审计规则相应调整**（区分「真人签名」与「模拟签名」）：
- 真人签名照片形态（`data:image/png;base64` / `data:image/jpeg;base64`）→ **仍拦截**（PII）；
- 程序生成的模拟签名（`data:image/svg+xml;base64`）→ **放行**。

### 验证

| 层 | 结果 |
|---|---|
| 本地冒烟 | **163 / 163 PASS**（新增 4 条签名图断言） |
| 深度数据审计（3 变体） | 全部通过（含「签名图必须是 SVG 而非 PNG/JPG」的新规则） |
| 线上断言 | **154 / 154 PASS**，0 FAIL |
| 签名图覆盖 | 4 签名人 × 横/竖两版；619 条点检记录 100% 带签名；温湿度记录员栏带签名 |

---

## v1.43.0（开源脱敏版 · 2026-09-24）

主人发来对比截图：左边演示站是「12 房间 / 6 区域 / 48 设备」按「第1组…第6组」分组，
右边工厂真机是「**15 房间 / 14 区域 / 124 设备**」按「**持久蠕变（101 台）+ 独立房间**」组织，
且每个房间卡片带**仪器型号与表单编号** —— 演示站的观感明显单薄。
要求：**以工厂最新源为准重建演示环境，脱敏后保持演示效果。**

### 一、结构对齐（这是本轮的主体）

| 维度 | 工厂真实 | 演示站（改前） | 演示站（**改后**） |
|---|---|---|---|
| 房间 | 15 | 12 | **15** |
| 分组 | 持久蠕变(1) + 独立房间(13) → 区域 14 | 第1组…第6组 → 区域 6 | **持久蠕变(1) + 独立房间(13) → 区域 14** |
| 设备 | 124 | 48 | **124** |
| 模板 | 19（显微镜无模板，走专项） | 9 | **17**（同样显微镜无模板，走专项） |
| 科室 | 力学 / 金相 / 化学 / 机加 | 演示科室一~四 | **力学 / 金相 / 化学 / 机加** |
| 房间卡片 | 型号 + 仪器编号 + 表单编号 | 只写「演示用恒温恒湿实验室」 | **型号规格 + 仪器编号 + 表单编号** |
| 无模板设备 | 4 台（全为显微镜，只挂「显微镜维护记录」专项） | 无 | **4 台**（3 金相显微镜 + 1 体视显微镜，全部挂显微镜维护记录专项） |

实测：房间页 KPI 从「12 / 6 / 48」变为「**15 / 14 / 124**」，与工厂完全一致；
大屏打开「蠕变试验室 1」铺 **81 张卡片**（设备总数 81、今日已点检 64、完成率 79%、本月累计 **1499**）。

### 二、脱敏红线没有放松（这是「保持效果」的前提）

| 真实值 | 处理 |
|---|---|
| 厂商型号 `RDL-50` `GWT2504` `CMT5205` `Qness 10A+` `Axio vert A1` `OLYMPUS SZX7` | **换成自造的规格级短码**：`CRE-50`（50kN 级电子蠕变机）、`HPT-1500`（1500℃ 高温持久）、`UTM-200`（200kN 万能机）、`HV-MICRO`、`MET-INV`…—— 既有型号质感又**不可反查厂商与规格** |
| 表单编号 `PK-JC-QR-032` | `DEMO-QR-032`（保留编号体系） |
| 仪器编号 `hx-rtu-t10/PK-JCZX-882` | `DEMO-RTU-T10/DEMO-ZX-882` |
| 人员姓名 | 演示员甲~辛（8 位，与 4 科室对应） |
| desc 里的厂商名（「中机 RDL-50」「MTS / 万测 / 钢研纳克」） | 换成规格描述（「50kN / 100kN 级电子蠕变试验机」「摆锤式冲击试验机」） |

**房间名保留了功能性名称**（蠕变试验室 / 冲击试验室 / 硬度室 / 金相分析室 / 落锤室 / 危化品室 /
ONH 分析室 / 碳硫分析室 / 直读光谱室…）—— 这类通用实验室叫法不含单位、人员或型号信息，
保留它们才撑得起演示效果（旧版把「持久蠕变」「金相分析室」也一刀切脱敏成「测试室01/02」是过度脱敏，
本轮已把这两个词从 PII 词表移除，并加了**真 PII** 的兜底：`RDL-`、`GWT`、`CMT`、`ZBC`、`TPS`、
`Qness`、`Axio`、`OLYMPUS` 等——任何真实型号出现在数据里都会被审计拦下）。

### 三、踩到的坑（记录给下次）

1. **播种脚本里的演示账号 dept 没同步**：生成器把房间 dept 改成力学/金相/化学/机加，
   但 `_seed_demo_auth.py` 里 `admin` 还是「演示科室一」→ `/api/env/alerts` 按 dept 过滤后
   **两个账号都看到空预警**（没有任何房间的 dept 叫「演示科室一」）。
   表现完全像「预警功能坏了」，实际是**账号与数据的科室对不上**。
   → 已改为 admin=力学、demo-user=金相（超限房间恰好分布在力学与金相两个科室）。
2. **专项检查的 device_ids 从旧 src 透传**：旧数据里的 `prg-demo079/demo102` 指向旧房间的旧设备，
   结构一换就全部失配（专项表挂错设备、checkRecords 数量掉到 12）。
   → 生成器改为**按房间型号清单重建**专项归属，不再从 src 透传。
3. **断言「表头设备清单行数 == 设备数 + 1」**：大房间（81 台）用「编号区间压缩」成 11 行，
   行数自然 ≠ 设备数。→ 断言改为「多行且包含设备编号」。
4. **`_seed_demo_auth.py` 的 EXPAND 还指向旧生成器**：换了新生成器但播种仍用旧的
   （`devices=48`），播种的是旧结构。→ 改成 v143。

### 验证

| 层 | 结果 |
|---|---|
| 本地冒烟 `_smoke_v142.mjs` | **161 / 161 PASS**（房间名/分组/科室改为从演示数据动态推导锚点，不再写死） |
| 深度数据审计（3 个变体） | 类型 / 枚举 / 引用 / 时间戳 / 通知 / 模板被使用数 / **PII（含真实型号）** 全部通过 |
| 线上断言 `_verify_v142.py` | **154 / 154 PASS**，0 FAIL |
| 房间页 KPI 实测 | **房间总数 15 · 房间区域 14 · 在册设备 124**（与工厂一致） |
| 大屏实测（蠕变室1） | 81 张卡片、今日已点检 64（79%）、本月累计 1499、铃铛 5 条未读 |

---

# 更新日志（CHANGELOG）· 设备点检系统（开源脱敏版）

> 本文件仅记录**开源脱敏版**的发布与变更。上游内部版本的逐版本迭代史不在此列出。
> 版本号沿用上游语义化版本规则：**主.次.修订** —— 接口破坏性变更→主+1；新功能→次+1；bug 修复→修订+1。

---

## v1.42.1（开源脱敏版 · 2026-09-24）

主人反馈「**演示里面缺少常规点检**」—— 查下来是**后台「点检模板」页签里所有设备点检模板都显示「被 0 台设备使用」**，
看起来就像演示数据根本没把常规点检配到设备上（而紧邻的 4 张专项表却正常显示「N 台设备参与」，对比非常刺眼）。

### 一、根因：`/api/templates` 漏了下发 `use_count`

上游 `handleListTemplates` 会现算「该模板被多少台设备使用」：

```js
const cnt = {}; allDevices().forEach(d => { if (d.template_id) cnt[d.template_id] = (cnt[d.template_id]||0)+1; });
allTemplates().map(t => Object.assign({}, t, { use_count: cnt[t.id] || 0 }))
```

只读 Worker 直接 `return json(ctx.kvget('templates', []))` —— **字段缺失，而前端写的是 `t.use_count || 0`**，
于是被**静默兜底成 0**。这类缺陷比 `undefined` 更难发现：**页面不报错、数字老老实实显示 0**。

除了显示，它还会让两处**语义校验失效**：
- 删除模板时「该模板仍被 N 台设备使用，请先给这些设备换模板」的拦截**恒不触发**；
- 编辑检查项时「该模板同时被 N 台设备使用，编辑会同时改变这些设备的点检表」的提示
  **恒显示成「该模板目前只被这台设备使用」** —— 直接误导用户以为改一处不会波及其它设备。

**修法**：在只读 Worker 的 `handleListTemplates` 里按上游口径现算并下发 `use_count`。

### 二、顺手抓到的 3 处同形态问题

体检思路：**前端所有 `xx || 0` 的兜底都是「字段缺失伪装成 0」的高危点**，
把这些字段名全列出来，逐个比对接口是否真的下发。据此又查出：

| # | 问题 | 现象 |
|---|------|------|
| 1 | 演示数据 `notifs[]` 缺 **`to`**（接收人 userId） | `/api/notifs` 的 `filter(x => x.to === u.id)` 把通知**全部过滤掉** → 铃铛恒 0 条、面板恒为空、预警横幅永不出现 |
| 2 | 同理缺 `alert_id`、`read` 用了 `true/false` | 无法与温湿度预警联动；`read` 统一为 0/1（与上游 `notifs.push` 一致） |
| 3 | 超限格只造在**演示科室二**（测试室06） | `/api/env/alerts` 是**按登录人科室过滤**的（**admin 也不例外**）→ 管理员登录后预警页全空 |

**演示数据的教训**：演示账号 `admin` 属演示科室一、`demo-user` 属演示科室二，
**凡是「按科室过滤」的功能，演示数据必须在每个科室都铺一份**，否则换个账号登录就是空白页。
现在测试室01（科室一）与测试室06（科室二）各有 8 条真实超限，任一演示账号登录都能看到预警与通知。

### 三、修一个我自己引入的 bug：`rng` 变量跨循环泄漏

上一轮我加了「温湿度按各房间范围生成」，实测**从未生效** —— 所有房间的数据都落在
49~61%RH / 22.7~24.3℃，正是**最后一个房间（测试室12）**的范围。根因：

```python
for (…, rng, …) in ROOM_DEFS:      # ← rng 只在这个循环里有效
    ...
for (yy, mm) in months:
    for ri, rm in enumerate(rooms):
        tMin, tMax, hMin, hMax = rng       # ← Python 的 for 变量泄漏到外层，这里拿到的是上一轮的残留值
```

不报错、只是数据全错。→ 改成显式查表 `ROOM_RNG[rm["name"]]`，并在注释里写明原因。
（**教训**：嵌套循环里绝不复用另一个循环的循环变量，哪怕它「看起来还在作用域里」。）

### 四、把「KV 边缘缓存」这个坑固化进流程

播种后跑验证时反复出现「明明改了数据，页面上还是旧的」—— 因为 **Cloudflare KV 是最终一致的，
且边缘节点各自缓存**：同一域名可能有节点读到新值、有节点还读到旧值（本轮实测：旧数据里超限在
演示科室二，于是 admin 读到的 counts 全是 0，看起来像代码 bug）。

两处加固：
- `_seed_demo_auth.py` 末尾**轮询演示站**（`/api/env-alerts`，读落库数组、不按科室过滤、无需登录）
  直到读到期望条数，直接确认「KV 已生效」；
- `_verify_v142.py` 对「由演示数据决定」的断言做**自适应等待**（最多等 2 分钟）。

### 验证

| 层 | 结果 |
|---|---|
| 本地冒烟 `_smoke_v142.mjs` | **159 / 159 PASS**（新增 use_count ×3 + notifs ×4） |
| 深度数据审计（3 个变体） | 类型 / 枚举 / 引用 / 时间戳 / 通知字段 / **模板被使用数** / PII **全部通过** |
| 线上断言 `_verify_v142.py` | **153 / 153 PASS**，0 FAIL |
| 实测取证 | 点检模板「被 12/6/12/3/4/4/3/2/2 台设备使用」（合计 48 = 设备总数，已无「0 台」）；通知铃铛 **🔔5 条未读** + 预警横幅有内容 |

---

## v1.42.0（开源脱敏版 · 2026-09-24）

本轮不是跟随上游升版，而是**对线上演示站做了一次全站体检并修复发现的缺陷 + 大幅扩充演示设备**。

**体检方法**：用 CDP 逐页开 29 个视图（含后台 14 个页签、4 个移动端视口），采集控制台错误、失败请求、
表格行数与「空状态」文案，最后逐张看截图。**技术层全绿（0 报错 / 0 失败请求）** ——
但正是这种「全绿」最容易掩盖问题：本轮 10 个缺陷里有 8 个是**静默降级**（页面照常 200、控制台干净，
只是显示成「—」「0」「undefined」「[object Object]」或筛选毫无效果）。

### 一、只读 Worker 的接口契约缺陷（7 处，全部是静默降级）

| # | 接口 | 缺什么 | 页面上看到的现象 |
|---|------|--------|------------------|
| 1 | `GET /api/inspections` | 响应缺 `room` 字段 | 后台「点检记录」的**房间列全部显示「—」** |
| 2 | 同上 | 未实现 `?name=` / `?signer=` | 「按名字筛选」输入任何值都返回全部记录（**静默无效**） |
| 3 | 同上 | 未实现 `?room=` | 「按房间筛选」选任何房间都返回全部记录（**静默无效**） |
| 4 | `GET /api/dashboard` | 未实现 `?group=` | 房间选择页点分组进入大屏，标题写着「合并视图：测试室01 · 测试室02」、**卡片却铺满全部设备** |
| 5 | 同上 | 缺 `job_total/job_done/job_abnormal/jobs` | 大屏两个核心 KPI 恒为 **「今日已点检 0 / 完成 0%（共 0 项检查）」** |
| 6 | `GET /api/rooms` | 缺 `env_range` | 后台房间管理看不到「🎲 专属范围」提示；温湿度预警的「范围来源」退回全局 |
| 7 | `GET /api/env-records` | 数据侧缺 `updated_at` | 温湿度页恒显示「尚未保存」（记录其实一直都在） |
| 8 | `GET /api/admin/logs` | 返回 `{logs:[]}` 而前端读 `rows/total/returned/files` | 日志页恒显示「**内存共 undefined 条 · 显示最新 undefined 条**」 |

第 4、5 项尤其值得记：**`?group=` 与 `job_*` 是同一个接口的两半**，缺哪一半都会让大屏看起来「坏了」，
而页面不会抛任何错。修法是照 `server.js` 的 `handleDashboard` 补齐任务口径（`weekKeyOf` / `progRowOf` /
`jobStatusFromRow` / `progRowSig` 四个纯函数一并移植），保证「一台设备同时跑点检表 + 专项表 = 2 张卡」的口径与上游一致。

### 二、演示数据的类型 / 枚举错误（4 处）

| # | 问题 | 现象 |
|---|------|------|
| 1 | `checkRecords` 的结论列写了**显示字符** `√`，而契约是内部码 `normal` | 专项检查页结论列渲染成字面量 **`undefined`**（前端 `CONC_MARK[v]` 查不到） |
| 2 | `programs[].side_items` 是**对象数组**，而前端按**字符串数组**渲染 | 右侧检查步骤栏显示 **`1. [object Object]`** |
| 3 | `signers[].sig_chars` 是**字符串**（应为签名笔画数组）、`sig_dir` 写成 `right`（取值只有 `v/h/auto`） | 后台签名人「字符数」读成字符串长度 |
| 4 | `signers[].name` 与 `signed_by` 混用简称 | 月度表签名栏拼出「演示签名人、丙」这种半截名字 |

统一为 **演示员甲 / 乙 / 丙 / 丁 / 戊**，`signed_by` 一律用全名。

### 二之二、演示数据的时区错误（这个是我自己写生成器时引入的）

大屏「最近点检记录」里，今天（14:35）的记录时间显示成 **`23:28:00`** —— **签在将来**。

- **根因**：真实服务端用 `new Date().toISOString()` 存 UTC，前端 `new Date(x).getHours()` 再按**本地**渲染。
  我的生成器把「本地小时」（8/9/10/14/15）直接写成了 `...T15:28:00.000Z`，前端按 UTC+8 一加就成了 23:28。
- **修法**：生成器显式按 `UTC+8` 构造本地时刻再转 UTC 存储；并且**今天的记录只允许取「当前小时之前」的小时池**
  （`hours_for(day)`），彻底避免「今天的记录落在将来」。`envRecords.updated_at` / `checkRecords.updated_at`
  同样处理（今天用「now − 30 分钟 / − 2 小时」）。
- **固化检查**：深度审计新增一条「时间戳不得落在将来」的断言。

### 三、开源补丁：旧 `.nav` 体系的移动端适配

上游实际存在**两套顶栏**：新版 `.app-nav`（`index/rooms/inspect`，`v2/theme.css` 里带 900px 折叠菜单）
与旧版 `.nav`（`env/check/admin`，样式在 `style.css`，**完全没有媒体查询**）。
在 390px 手机上，旧版顶栏的链接被压成竖排，并且——

> **`<nav class="nav">` 的最小内容宽度是 417px**，它把整个**布局视口**撑到 417px
> （实测 `window.innerWidth=417` 而 `visualViewport.width=390`）。
> 这同时解释了两个看起来无关的现象：① 顶栏文字崩坏；② 整页横向溢出 27px、**固定定位的只读横幅被拉宽到 417px**。
> 排查时先用「逐个隐藏元素看 `innerWidth` 是否回落到 390」二分定位，一击命中 `nav`。

修法（`_build_v142.py` 的 `patch_mobile_nav()`，**三个变体都打**，不只演示站）：
- 新增 `assets/nav-mobile.css` + `assets/nav-mobile.js`；给旧 `.nav` 注入 `<button class="nav-toggle">☰</button>`；
- ≤760px 折叠：只留「品牌 + ☰」，其余 `display:none`（**这一步同时消掉了 417px 的最小宽度**）；
  展开后链接竖排铺满整行；桌面端完全不受影响（`.nav-toggle` 默认 `display:none`）；
- 顺带修掉「粘性顶栏在滚动时会被 fixed 横幅盖住」：横幅把自身高度写成 CSS 变量 `--ro-banner-h`，
  `.nav` / `.app-nav` 的 `top` 顺延到它下方（窄屏横幅会换行成 2~3 行，高度是变的，所以每次 resize 都更新）。

### 四、演示设备大幅扩充（主人明确要求）

| 项 | 原来 | 现在 |
|---|---|---|
| 设备 | 8 台（名字重复：「电子式高温蠕变持久试验机 01/02/06/07」） | **48 台**（10 类设备，编号 DEMO-001~048 全局唯一，按房间连续编号） |
| 房间 | 8 个（其中 4 个没有任何温湿度数据） | **12 个**（分 6 组、4 科室；**每间都有设备、有整月温湿度**） |
| 模板 / 专项检查 | 5 套 / 2 张 | **9 套 / 4 张**（新增硬度计、落锤、晶间腐蚀、摩擦磨损等 4 套模板；新增 2 张专项表，覆盖月 / 周两种行单位） |
| 签名人 | 3 位（半截名字） | **5 位**（演示员甲~戊） |
| 点检记录 | 12 条（月度表 30 天里只有 1~12 天有零星数据 → **整月几乎是空表**） | **1511 条**，覆盖基准日前 40 天（月度表满格、工作日接近全量、周末减量、异常率 3.5%） |
| 温湿度 | 4 个房间、17/15/15/2 个格子 | **12 房间 × 本月与上月 × 每时段**，共约 4000 个格子 |
| 异常 / 通知 | 1 条 / 0 条 | 62 条 / 6 条 |
| 预警记录 | 2 条 | 8 条（与超限格一一对应） |

> ⚠️ **温湿度取值按房间范围生成**：第一版把所有房间的基线都写死成 `20 + ri%5`，结果测试室01
> （要求 20~25℃）被刷出 **93 条「温度低于下限」预警**，看着像数据坏了。改为**取各房间范围的中点 ± 小噪声**
> （常规格子绝不越界），只在测试室06 刻意造 8 条真实超限 —— 演示数据要的是「**有几条**真实预警」，不是「全是预警」。

**⭐ 两个基准日，各司其职**（这是本轮最重要的设计）：
- **仓库种子** `data/sample.kv.json` 锚定固定基准日（`2026-09-24`）→ 自托管用户拿到确定性、可复现的样本；
- **线上演示 KV** 在播种时**按「今天」重新生成**同一套数据 → 否则「今日已点检 / 最近点检记录」永远是空的，
  大屏 KPI 显示 0，演示站看起来像坏了（**这正是体检时看到的第一个现象**）。
两处由同一个带 `--ref` 的生成器产出，口径完全一致。

体积实测：种子 843KB（其中 `inspections` 298KB、`envRecords` 225KB）；`JSON.parse` **仅 2ms** —— 
对照工厂真实 `data/kv.json` 是 **28MB**，演示数据完全在安全范围内（Workers 免费版 10ms CPU）。

### 五、新增质量门禁

- **深度审计脚本**：把本轮教训固化成可执行检查 —— **不只审「字段是不是数组」，还要审「元素类型」**，
  外加枚举（`conclusion` 必须内部码）、`sig_chars`/`sig_dir`、温湿度单元格键格式与数值形态、
  **引用完整性**（设备→模板/房间、记录→签名人/专项表是否存在）、**时间戳不得落在将来**、PII 兜底。
  三个变体全部通过。
- **冒烟新增 40+ 项断言**：覆盖上述每一个修复点（含「无效名字/无效房间必须返回 0 条」这种**反向断言**——
  只断言「筛选生效」不够，还要断言「不该匹配的一律不返回」，否则「静默返回全部」这种缺陷仍会漏过）。
- **改掉 5 处写死的断言**：`env-alert-cfg` 的 `effective.length === 8`（房间扩到 12 个）、
  `programs 数量 === 2`（扩到 4 张）、`env-alerts === 2`（变 8 条）、
  「测试室04 设备清单 8 行」（改 6 台）、「检查记录改期 `__date=16`」（改为「存在任意一条改期行」）——
  **「断言要基于读到的实际值，不要基于自己以为的默认值」**（上游 v1.37.2 / v1.39.0 都栽在这上面过）。

### 六、两个「自己给自己挖坑」的教训

1. **`async` 前缀残留**：用整段替换的方式改 Worker 里的 handler 时，原文是 `async function handleX(ctx) {`，
   而我的替换串从 `function handleX` 开始 —— 结果留下一个孤零零的 `async`，文件变成
   `async // 注释...` → `ReferenceError: async is not defined`。**同一轮踩了两次**。
   → 全局加了一条排查：`^\s*async\s*//` 或 `^\s*async\s*$`。
2. **冒烟脚本崩了但旧输出文件还在**：脚本因语法错误在写文件之前就退出，而我去读**上一次的**
   `_smoke_v142_out.txt`，看到 `ALL_SMOKE_PASS` 差点误判。→ 现在跑之前一律先 `rm -f` 输出文件，
   并检查 `exit code`；**「读旧产物」是最容易自欺欺人的一种验证失效**。

### 验证

| 层 | 结果 |
|---|---|
| 本地冒烟 `_smoke_v142.mjs` | **152 / 152 PASS** |
| 深度数据审计（3 个变体） | 类型 / 枚举 / 引用完整性 / 时间戳 / PII **全部通过** |
| 线上断言 `_verify_v142.py` | **145 / 145 PASS**，0 FAIL |
| CDP 全站体检（29 个视图，含 14 个后台页签 + 4 个移动端视口） | 控制台错误 **0**、失败请求 **0**、空状态提示 **0** |
| 移动端溢出专项 | 修复前 env 布局视口 **417px**（溢出 27px）→ 修复后 9 个页面**全部 0 溢出** |
| 只读写入口审计（15 个页签） | 可见写入口 **0** |
| 大屏 KPI 实测 | 分组视图「完成 **67%**（共 15 项检查）」/ 单房间「完成 **56%**（共 9 项检查）」，今日记录时间全部在「现在」之前 |

---

## v1.41.0（开源脱敏版 · 2026-09-23）

跟随上游 v1.37.6 → **v1.40.0**（跨 v1.37.7 / v1.38.0 / v1.39.0 / v1.40.0）重新脱敏与同步。上游这一轮是**一个大功能 + 一个真 bug 修复**：新增「数据定时备份到本机文件夹」，并修掉 LIMS 抓取因跨度超限必然失败的问题。

**同步上游能力**

- **数据定时备份（v1.38.0 → v1.39.0 → v1.40.0，三轮迭代）**：把全量数据定时自动落盘到本机指定文件夹，`年/月` 自动分层归档，文件名 `点检数据备份_YYYY-MM-DD_HHmmss.json`，并**同时落一份配套的 `*.config.json`**（内含 `PEPPER` 密钥）。这一条很关键：只还原业务数据而缺这份密钥，用户口令校验会对不上，等于白还原。
  - **v1.38.0**：单目标 + 每周一次 + 全部保留。服务内置 `setInterval` tick（每 30 秒一次，触发窗口 5 分钟），不需要外部计划任务、不需要联网；备份内容取**内存里的 `store`**（而不是直接拷 `data/kv.json` —— `scheduleSave()` 有防抖，直接拷盘可能拿到上一次的旧数据）；落盘用「先写 `.tmp` 再 `rename`」原子替换；失败可重试 3 次（3s/6s/9s）。
  - **v1.39.0**：升级为**多目标文件夹**（上限 8，同路径自动去重），**序列化 + 压缩只做一次**、多目标共用同一份字节 —— 既省 CPU，也保证各处副本**逐字节一致**；每个目标独立重试、独立保留、独立成败（只要有一个写成就算成功，部分成功以 `ok_count/target_count` 记录并逐条列出是哪个目标没写成、为什么）；新增**备份频率可调**四档（每周 / 每天 / 每隔 N 小时 / 每月第 N 日），并抽出 `backupPeriodKey()` 统一产出周期键。
  - **v1.40.0**：把频率**拆到每个目标各自一套**（`targets: [{dir, enabled, freq, weekday, day, hours, time, keep}]`），同一时刻多个目标到点则**合并成一次写**；**判重台账改为逐目标**（`lastAuto[<目标绝对路径小写>]`，落盘、跨重启有效），且**只有成功才封该目标的周期**（失败不封，好让窗口内的下一轮 tick 再试）；新增纯函数 `backupNextFor()` 逐目标算「下次执行」，全局那个取所有启用目标里**最早**的一个。
  - **两条刻意的设计取舍**（值得单独记住）：
    - 「**何时留一份、留几份**」可以逐目标设，「**怎么写这一份**」必须全局统一 —— 因为多目标共用同一份序列化字节是「各副本逐字节一致」的基础，若各目标压缩方式不同就得各生成一份，这个性质就没了。
    - **手动备份不参与判重**，只有自动备份成功才封周期 —— 否则周一随手手动备过一次，本周日的自动备份就被跳过了，与「每周定时」的承诺不符。
  - **界面**：后台新增「💾 数据备份」页签，一个目标一张卡片（各自一整套策略 + 实时状态行：可写入 / 磁盘余量 / 下次执行）；状态卡顶部频率标签从「频率：」改成「**默认频率：**」（它现在是「新增目标时继承的默认值」，与下面逐目标的「频率：」并列会让人以为改它就能改所有目标）。
  - **兼容**：老结构 `dirs: ['D:\\a','E:\\b']`（多目录共享一套频率）**自动虚拟迁移**成「每个目标各自继承那套频率」，读操作不写 kv，升级后行为不变。
- **LIMS 抓取「超时」根因修复（v1.37.7）**：症状是工厂机 LIMS 抓取温湿度超时，但**其实不是网络超时，是 LIMS 的业务错误码**。排查链：LIMS 首页 `GET /` 21ms 返回 200（服务本身很快）→ `POST /api/lims/test` 返回 `login_ok:true` 但 `ok:false`，`error: "LIMS humidityChart code=5004 时间间隔过长"` → 逐档实测边界非常干净：**24 / 25 / 31 天 ❌，23 天 ✅，20 / 15 / 10 天 ✅** ⇒ 单次查询跨度**上限 23 天**，而抓取代码是**一次拉整月**，28~31 天必然被拒。
  - 修法三处：① `limsFetchHumidity` 拆成 **≤20 天/段**串行拉取后合并去重（留 3 天余量，LIMS 再收紧一天也不会立刻炸）；② 房间名探针的 `days` 上限 31 → 20（否则用户选「回看 30 天」会把**房间名完全正确**的房间误报成「LIMS 中不存在」）；③ `handleLimsVerifyRooms` 的 clamp 同步收紧。另把 `5004` 标为**不可重试** —— 同一跨度重试永远不会成功，原来会白等 2 次退避（约 3.6 秒）再报同样的错。

**开源脱敏版侧**

- 基于上游 v1.40.0 重新执行脱敏流水线（`_build_v141.py`）：3 个变体 × 12 个 HTML/JS + `server.js` 全量重扫，8 项 server.js 补丁全部命中，只读横幅递归注入 11 个页面，`manual.html`（含真实界面截图）继续排除并保留中性说明页。
- ⚠️ **本轮新抓到一处真实泄露**：上游 v1.37.7 新增的注释里带出了**现场主机的裸内网 IP**（`实测（2026-09-23，工厂 192.168.0.141）`）。旧的替换表只覆盖「带 `:4001` 端口的完整 URL」，裸 IP 会漏网 —— 已补 `工厂 192.168.0.141 → 现场 LIMS 服务` 与兜底 `192.168.0.141 → LIMS 服务地址`（顺序放在带端口的规则**之后**，避免先被短规则拆掉）。
  同时把复核脚本从「已知 token 匹配」升级为「**已知 token 匹配 + 泛化清点**」两层：后者列出所有内网 IP / 盘符路径 / 疑似凭据赋值 / 内嵌 base64，供人工判读。**固定的黑名单只能抓到已知的，上游新写进去的未知形态必须靠泛化清点兜住。**
- 只读 Worker 同步（v1.41.0）：
  - 新增 `GET /api/backup/config` 与 `GET /api/backup/files`，**忠实复刻上游响应形状**（`cfg / targets[] / default_policy / summary / history / run_last / next_run_local / weekdays / freqs / counts`…）。漏补会怎样：前端 `catch` 掉异常 → 页签**照常 200、无报错、控制台干净**，只是整块空白 —— 这类「静默降级」是最难发现的，必须靠「路由 diff 抓新增 GET + 扫前端 `/api/` 字面量」两步固定流程防住。
  - ⚠️ **Workers 没有文件系统，所以这两个接口不做任何伪造**：目标文件夹的可写状态与磁盘余量一律**如实报「不可用」**（`writable:false` + `write_error` 说明原因），绝不假装可写；「已生成的备份文件」由演示数据里的 `history` 推导（该接口本来的设计意图就是「与历史记录相互印证」）。响应带 `readonly:true` 与 `demo_note` 说明这一点。
  - 顺带把上游的 LIMS 跨度拆分也移植进 Worker（`LIMS_SPAN_MAX = 20` + `limsFetchHumidityWindow` + 5004 不可重试）。虽然演示站的 `/api/lims/sync` 是 POST、一律 403 走不到这段，但保留它是为了「形状与行为一致」，将来若放开演示站写入时不会丢掉这道修复。
- 演示数据：新增 `kv.backup`（**2 个目标**：一个留空 = 默认目录、一个 `E:\设备点检备份`；逐目标频率分别为 `weekly` / `monthly`，正好演示「各自独立」）+ 3 条执行历史（含 1 条**部分失败**，演示「1 坏 1 好仍算成功并列出原因」）+ `lastRun` + 逐目标判重台账。
  - `enabled` 刻意设为 **`false`**：`sample.kv.json` 是**三套版本共用**的种子数据，若默认开启，任何人 clone 下来跑一次 Node 版就会真的往目标文件夹写备份。
  - ⚠️ **只在部署时注入演示 KV**（`_seed_demo_auth.py`，与演示账号 / `pepper` 同一套路）：线上演示站把总开关打开成 `true`，纯粹为了展示逐目标频率与「下次执行」——只读站在入口就 403 掉所有写方法、运行环境也没有文件系统，开与不开都不可能真的写盘。开源种子文件保持 `false`。
  - 台账的 `periodKey` 由脚本按**真实日期推导**（`2026-09-21` → `2026-W39` / `2026-09`），与「最近一次执行成功」自洽；写死一个过期的周期键会让前端把它显示成「上一周期，本周期未备」，那是另一种真实状态，用在成功记录上就自相矛盾了。
  - 温湿度数值规范化与**数组型字段类型审计**照常跑（`programs.criteria/columns/side_items`、`templates.items`、`backup.targets/history` 等），本轮 `0 处异常`。
- 文档：README / 功能说明 / 更新日志 / version.json 全部升 v1.41.0；功能说明新增 `backup` 子结构完整字段表与周期键说明；README 补「演示站上数据备份接口的语义边界」与「`wrangler.toml` 占位符不得作为静态资源上传」两条告警。

**验证**

| 层 | 结果 |
|---|---|
| 本地冒烟 `_smoke_v141.mjs`（直接 import 只读 Worker + mock KV） | **113 / 113 PASS** |
| 线上断言 `_verify_v141.py` | **117 / 117 PASS**，0 FAIL |
| CDP 运行时（逐页截图 + 控制台错误） | 4 页（含登录态后台「数据备份」页签），控制台错误 **0** |
| CDP 只读写入口审计（15 个页签逐一点开） | 可见写入口 **99 → 0** |
| 全仓敏感 token 复扫 | 真实 KV ID / 令牌 / 品牌 / 姓名 / 型号 / 内网 IP **零残留** |
| 反向校验 | 工厂真实数据/备份路径（写成可读形式会被本文件自身命中，故此处不逐字列出）**0 处**；已废除的 `height:9.2mm` / `height:12.8mm` **0 处** |

**顺带修掉一个真实缺陷：只读横幅的写入口隐藏漏了「动态渲染」的按钮**

- **现象**：登录演示站后台逐页签审计，页面上仍残留 **99 个可见的写入口** —— 设备/房间/科室/模板/检查记录/用户每一行的「编辑」「删除」、检查表页签成排的「保存 / 恢复默认」、数据备份目标行的「✕」等，点了都只会弹 403 提示。
- **根因**：横幅的 `hideWrites()` 只在固定时刻（`DOMContentLoaded` / 400ms / 1200ms / 2500ms）跑一遍，而**后台几乎所有按钮都是「拿到接口数据后才动态渲染」的** —— 时点跑完之后新建的 DOM 永远不会被隐藏。
- **修法**：加 `MutationObserver` 监听 `childList + subtree`，任何后续渲染都触发一次防抖（150ms）重扫。
  - ⚠️ 观察项**刻意不含 `attributes`**：`hideWrites()` 自己会改 `el.style.display`，若把属性变更也纳入观察就会自我触发、形成死循环。
- **顺带补两条命中规则**：① 除 `textContent` 外**也看 `title`** —— 行内「✕」按钮文字只有一个符号，只能靠 title（"删掉这个目标" / "从清单移除"）识别；② 动词表补 `删` / `移除` / `测试`（新增的「测试全部写入」此前漏网）。
- **复核方式**：用 CDP 逐个点开 15 个页签 → 在页面上下文里跑审计脚本，判定用 `el.checkVisibility()` 而**不是** `getComputedStyle(el).display` —— 后台所有页签的 DOM 同时存在，只有当前页签容器可见，只看元素自身会把「看不见但存在于 DOM」误判成「可见」（第一版探针就是这样误报了 1100 个）。

**仓库维护**

- 新增 `.gitattributes`（`* text=auto eol=lf`）：本机 `core.autocrlf=true` 且此前没有该文件，Git 重新检出时会把工作区文件转成 CRLF —— 而脱敏流水线是用**多行精确字符串**给 `server.js` 打补丁的（`loadStore` 示例数据兜底 / `ensureUsers` 演示账号 / favicon 路由），CRLF 会让匹配**静默失配**（只打印一行「未匹配」而产物照样生成，等于把未脱敏代码当成品交付）。钉住 LF 彻底消除这个隐患。

---

## v1.38.0（开源脱敏版 · 2026-09-22）

跟随上游 v1.37.1 → v1.37.6（跨 v1.37.2 ~ v1.37.6）重新脱敏与同步。上游这一轮几乎全部围绕**打印可读性**与**写入侧数据校验**。

**同步上游能力**

- **写入侧数值收口（v1.37.6）**：温湿度保存新增 `numOrNull()` —— 只接受「空串 / 可选负号 + 数字 + 可选小数」，非法值把该字段**清空**并把 `rejected:[{cell,field,value}]` 回传前端，同时写入服务日志。根因是一处真实数据污染：某格 `humidity` 里存进了记录员姓名；而旧实现只做 `String(v).slice(0, 8)`，任何文本都能落进数字列，预警逻辑用 `parseFloat` 判断时又只是**静默跳过**，所以能长期潜伏。前端另加两道：输入时即剔除 `[^0-9.\-]`（人名根本打不进去）、保存后若被拒则 toast 逐条列出「哪一日 / 哪个字段 / 原值」。
- **数值统一一位小数（v1.37.4）**：服务端新增 `round1()`（`58 → "58"` 整数不补零、`58.3333 → "58.3"`、非数字原样放行交给上层报警），应用于 LIMS 填充的两条分支共 4 个赋值点；前端 `env.html` 的湿度改用与温度同一个 `fmtTemp()` 渲染，保证整列数字对齐。修复前温度走一位小数、湿度原样，长小数会把整列顶歪。
- **打印线宽全站统一为三档（v1.37.2 → v1.37.6）**：外框 `4.5pt` ／ 表格内线 `2.5pt` ／ 表单填写横线 `1.5pt`，覆盖温湿度、设备点检、批量打印、月报、专项检查 5 个页面；专项检查页此前**完全没有外框**，本次补上。原先是内线 `0.6pt`、划线短横线 `1px`（与内线差 3.3 倍），用户描述为「粗细不一致造成的不和谐感」。
- **两条值得单独记住的实测结论**：
  - **Chrome 对表格边框做宽度量化**（`border-collapse:collapse` 下吸附到固定档位）：声明 `0.6 / 0.75 / 1 / 1.25 / 1.4pt` 渲染结果**完全相同**（实测 0.61pt），`1.5~2pt` 是 1.16pt，`2.5pt` 才跨到 1.70pt。所以「把 0.6pt 改成 1pt」这种最直觉的修法**毫无效果**，必须跨过阈值。
  - **「纸上够清晰」≠「预览够清晰」**：验收时若用户是看 Chrome 打印预览判断的，就要按**预览尺度**（A4 横向缩到约 690px，缩放 0.61）评估 —— 线宽不足 1.4 个屏幕像素就会被抗锯齿摊成浅灰，看起来「像没渲染」。
- **打印分页根因更正（v1.37.6）**：上一版把「行高只有声明的 0.78 倍」归因为「Chrome 压 td 高度」，并据此做了 ÷0.78 补偿 —— **数值碰对了，归因是错的**。真因是容器固定 `1352px`（= 357.7mm）超过 A4 横向可用宽 281mm，Chrome 触发 **shrink-to-fit 把整页等比缩小 0.7856 倍**，高度、列宽、线宽全被缩，只是表格宽实测「满宽」才被误判成「只压高度」。修法是把容器钉到 `1056px`（= 279.4mm）消掉缩放，列宽改**整数 px 且总和严格等于表格内容宽**（`1048px`），外框取 `4.5pt`（= 6px 整数），并**撤掉**那条补偿。
- **签名图缩到 `14mm × 5mm`**：签名图原先声明 `height:8mm`，会把所在数据行撑到约 8.7mm 并**覆盖行高声明** —— 这解释了为什么「给数据行减高」对某些房间完全无效（压到 4.6mm 行高仍是 8.7mm），也解释了同一份 CSS 下各房间表格高度能差 80mm。缩到 5mm 后小于行高 7.2mm，行高重新由数据行统一控制。

**开源脱敏版侧**

- 基于上游 v1.37.6 重新执行脱敏流水线（`_build_v138.py`）：3 个变体 × 12 个 HTML/JS + `server.js` 全量重扫；内部文件代号、真实姓名 / 房间 / 型号 / 资产号、内网地址与凭据复扫清零；`manual.html`（含真实界面截图）继续排除并保留中性说明页；只读横幅递归注入 11 个页面。
- 只读 Worker 同步：`handleSaveEnvRecord` 补齐 `numOrNull` + `rejected` 回报（演示站本身在入口就 403 所有写方法，保留该校验是为了与上游形状一致，也防止将来放开演示站写入时丢掉这道收口）；新增 `round1()` 并替换 LIMS 填充里两处 `Number(x).toFixed(1)`；版本号升 v1.38.0。
- 演示数据：补齐 `kv.formHeads`（4 档）与 `programs[].criteria`（缺失字段），并把 77 处温湿度单元格规范化为一位小数形态；另加一条**类型审计**，对 `programs.criteria/columns/side_items`、`templates.items` 等数组型字段逐条断言 —— 这类「字段不是数组」的问题在 v1.32.1 轮次曾导致检查表**整页空白**，是演示数据最需要防的坑。

---

## v1.37.1（开源脱敏版 · 2026-09-21）

跟随上游 v1.36.2 → v1.37.1（跨 v1.37.0 / v1.37.1）重新脱敏与同步：

**同步上游能力**
- **表单页眉统一配置（v1.37.0）**：新增 `kv.formHeads`（按表种登记）与后台「🏷 表单页眉」页签；温湿度 / 设备点检 / 专项检查 / 批量打印四档页眉（表单编号 + 版本）统一维护，并支持**房间级覆盖**（`rooms[].form_head`）。解析优先级为「后台配置 > 载体自带（模板 / 检查表的 `form_code`、`form_rev`）> 内置默认」；**未配置档位输入框留空**即走默认，避免把默认值固化成配置后系统改默认再也不生效。
- **新增接口**：`GET /api/form-heads`（四档生效值 + 显式配置值 + 房间映射）、`PUT /api/form-heads`、`POST /api/form-heads/reset`（后两者为写方法）。
- **下发点扩展**：`/api/monthly` 每台设备带 `form_head`，响应新增 `sig_assets` 与 `form_head_checkall`；`/api/programs` 带 `form_head`；`/api/rooms` 带 `form_head`。
- **打印分页修正（v1.37.1）**：温湿度表打印时页眉不再独占首页 —— 根因是 `break-inside:avoid` 叠加百分比列宽累加出的**亚像素尾差**导致分页判定落到「溢出」一侧，修法是宽度钉成整数 px，并把日行行高收口留出余量。
- **下拉可读性（v1.37.0）**：原生 `select` 展开列表由操作系统绘制、**忽略 `select` 的 color**，故改为给 `option` 显式声明前景/背景；暗色页与白底页**各自声明**（否则一套规则会污染另一套）。另修 `optgroup` 顺序（规范要求分组必须排在普通 option 之前）。
- **全站对比度达标**：按 WCAG 公式逐处实算修掉真实缺陷（暗色页主按钮 2.20:1 → 5.17:1、v2 占位符 4.0:1 → 5.1:1 等）。
- 另修：`env.html` 未选房间时不再发空 `room=` 请求（消掉长期控制台报错）；`index.html` 搜索框下方会下坠的空下拉钉死。

**开源脱敏版侧**
- 基于上游 v1.37.1 重新执行脱敏流水线：3 个变体 × 12 个文件与 `server.js` 全量重扫；内部文件代号、真实姓名 / 房间 / 型号 / 资产号、内网地址与凭据再次清零；`manual.html`（6.3MB，内嵌真实界面截图）继续排除并保留中性说明页。
- 只读 Worker 同步：新增 `GET /api/form-heads`（含四档解析与房间映射）；`/api/monthly` 忠实补齐上游形状（原先缺少 `location/sig_assets/form_head*` 等字段）；`/api/programs`、`/api/rooms` 补 `form_head`；版本号升 v1.37.1。只读策略不变（写方法一律 `403 {readonly:true}`）。
- 演示数据：表种级页眉刻意保持「**未配置**」状态以展示上游的留空语义（`cfg_*` 为空、走内置默认），另给「测试室01」一个房间级覆盖（`DEMO-QR-008B Rev.A0`）用于演示逐房间差异。

---

## v1.36.2（开源脱敏版 · 2026-09-19）

跟随上游 v1.32.1 → v1.36.2（跨 v1.33.0 ~ v1.36.2）重新脱敏与同步：

**同步上游能力**
- **UI / 交互改版（v1.33.0，v3 提案落地）**：设备大屏卡片状态徽章 + 状态图例；一键点检「正常 / 异常」整行滑块 + 实时计数 + 粘性表头；管理后台 12 个页签按「基础档案 / 业务数据 / 温湿度 / 系统」四组分行；温湿度页段式选择器 + 常驻「保存 / 打印」主按钮 + 「更多操作 ▾」收纳（批量签名 / 划线 / LIMS 抓取 / 导出）。
- **顶栏「外部系统」快捷跳转（v1.34.0）**：数据模型 `kv.links.items = [{key,name,url,depts,enabled}]`，按登录人科室过滤（科室为空 = 所有登录人员可见），管理员恒可见；后台「系统 → 外部系统跳转」可增删改并保存。
- **操作手册内置（v1.35.0 / v1.36.0）**：手册由系统托管于 `/manual`，顶栏新增「📖 操作手册」入口，手册内目录 / 章节 / 交叉引用全链路可跳转；页面内地址动态取当前访问地址，不再写死。
- **手册入口后台可配（v1.36.0 / v1.36.1）**：`kv.links.manual = {enabled}`；`PUT /api/admin/settings` 支持「只改开关、不带条目列表」也能生效。
- **登录后顶栏去公司图标（v1.36.2）**：改为 CSS 绘制的方块标记 + 中性文字「设备点检系统」，零图片。
- **布局修复（v1.33.1）**：后台月度点检「检查项目」下拉溢出压住「月份」的问题修复，并加通用护栏防止控件内联宽度撑破列宽。

**开源脱敏版侧**
- 基于上游 v1.36.2 重新执行脱敏流水线：3 个变体 × 12 个文件（含新增的 `v3/v3-preview.html`）与 `server.js` 全部重扫；内部文件代号、真实姓名 / 房间 / 型号 / 资产号、内网地址与凭据再次清零。
- **操作手册未纳入开源版**：上游 `public/manual.html`（6.3MB，内嵌 18 张真实界面截图）无法通过文本替换脱敏，已从仓库排除；改放**中性说明页**（`/manual` 仍可访问）。自托管版本可自行放入完整手册，路径与入口不变。
- **外部系统跳转地址脱敏**：默认条目中的内网地址（LIMS / 力学工具箱 / BPM）替换为示例域名（`https://*.example.com`）。
- 只读演示站同步：`GET /api/settings` 增加 `links`（外部系统跳转 + 手册入口开关），使顶栏入口在演示站可正常渲染；只读策略不变（仅放行 `POST /api/admin/login`，其余写方法一律 403）。
- 演示数据补 `links`：4 条示例条目 + 手册开关，其中「力学工具箱」限定科室，用于演示「按科室可见」。
- 文档：README / 功能说明 / 更新日志 / version.json 全部升到 v1.36.2。

---

## v1.32.1（开源脱敏版 · 2026-09-18）

跟随上游 v1.29.0 → v1.32.1 的四个版本重新脱敏与同步：

**同步上游能力（v1.30.0 ~ v1.32.1）**
- **温湿度湿度区间**：新增「湿度：40~70%RH」区间写法，与「湿度：≤70%RH」上限写法并存；区间写法同时判定上下限，超限提醒与填充随机区间都按上下限取值。
- **温湿度表头设备清单**：后台「被监测的设备型号和编号」改为多行录入（textarea + 实时条数预览），表头行高随设备条数自动撑开，去掉 `nowrap/ellipsis` —— 设备多时不再被静默截断（打印件同样完整）。
- **打印可读性**：内容列自动换行、行高上限提升、签名图放大、页边距收窄，长检查项不再裁切。
- **周检项目手动改期**：行固定为每周周一，日期格可改选当月任意一天（占用/未来日期禁选）；行身份仍是周一，仅用 `__date` 覆盖显示日期，改回周一自动清除。
- **检查表下拉样式**：深色主题下日期/内容下拉的「白字白底」问题修复。
- **签名默认登录人**：用户可绑定「默认签名人」（`signer_id`），点检 / 检查表 / 温湿度的签名下拉默认选中本人。
- **批量点检权限**：普通用户在后台勾选「批量点检操作」后即可看到并使用一键点检面板。
- **签名人科室可编辑**：后台编辑签名人时科室下拉正确回显与保存，不再被静默清空。
- **预警跳转定位**：报警横幅每条整行可点击直达对应房间。
- **上游缺陷修复同步**：启动迁移不再覆盖「内容」列的自定义选项（改为「补缺不删增」）；系统更新提示不再因硬编码版本常量而反复弹出。

**开源脱敏版侧**
- 基于上游 v1.32.1 重新执行脱敏流水线：前端 9 个页面 + 3 个静态脚本、`server.js` 全部重扫；内部文件代号、真实姓名 / 房间 / 型号 / 资产号、内网地址与凭据再次清零。
- 只读演示站同步升级到 v1.32.1：补齐 `/api/env-alerts`、`/api/lims/job/:id`、`/api/lan-upgrade/ping` 三个读取接口；`/api/env/alerts` 改为与上游一致的「按房间要求逐格判定」实现；`/api/programs` 附带设备数 / 房间清单；`/api/check-records` 支持 `device_id` / `ym` 过滤；`/api/admin/me` 与 `/api/users` 补 `dept` / `signer_id` / `perms` 字段。
- 演示数据补齐新结构：温湿度区间与多条温度标准示例、多行设备清单（8 台）、周检项目（含「4,5」选项）、含改期的检查记录、预警记录与站内通知。
- 只读策略不变：可登录浏览（`admin/admin123`、`demo-user/demo123`），服务端对所有非 GET 请求返回 `403 {readonly:true}`；含密钥的 `/api/admin/backup` 需登录。

---

## v1.29.0（开源脱敏版 · 2026-09-15）

首个面向公开仓库的脱敏开源发布，基于上游 v1.29.0 代码整理：

- **全面脱敏**：移除全部敏感信息与内部标识（真实人员姓名、设备编号、房间名称、内网地址、业务数据、内部表单编号）。
- **去品牌化**：删除所有公司图标（logo / favicon / apple-touch-icon）与品牌文字；前端品牌区统一为中性文字「设备点检系统」，favicon 替换为中性 SVG。
- **演示站只读（可登录浏览）**：Cloudflare Pages 演示站为纯展示模式，访客无需登录即可浏览，也可用演示账号（`admin` / `admin123`、`demo-user` / `demo123`）**登录后台查看**；系统拦截一切写入操作（新增 / 编辑 / 删除 / 签名提交 / 批量 / 备份恢复 / 远程升级等），服务端对所有非 GET 请求返回 `403 {readonly:true}`。唯一放行的写方法是登录（只校验口令并签发 cookie，不落库）；含密钥与口令哈希的整库导出 `/api/admin/backup` 额外要求有效登录会话。
- **演示数据**：内置虚构 `data/sample.kv.json`（设备 / 模板 / 房间 / 签名人 / 点检记录 / 温湿度 / 独立检查项目等），首次启动自动生成运行时数据。
- **凭据安全**：`PEPPER` / `SECRET` 首次启动随机生成并写入已忽略的 `data/config.json`；LIMS 地址 / 账号 / 密码均为占位符，默认关闭。
- **三套形态**：Cloudflare Pages 只读演示站、Node 服务版（完整功能）、Windows 便携版（完整功能）。
- **文档**：新增 README、LICENSE（MIT）、功能说明、本更新日志。

---

## 功能覆盖（v1.29.0）

设备台账、点检模板、点检作业与大屏、电子签名、月度点检表、温湿度监测记录与报警、独立检查项目（programs）、点检记录（checkRecords）、通知（notifs）、细粒度权限、LIMS 对接与局域网远程升级（接口保留、默认关闭）。

---

## 计划 / 说明

- 开源版的迭代将聚焦：文档完善、演示数据丰富、部署脚本简化，以及在不带入真实数据前提下的功能演示增强。
- 任何涉及真实业务数据、内部网络或第三方系统凭据的内容均不会进入本仓库。
