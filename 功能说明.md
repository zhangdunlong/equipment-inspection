# 功能说明（开源脱敏版 · v1.42.1）

本文档说明系统的功能模块、数据模型与接口契约。所有示例数据均为虚构脱敏内容，不含真实业务信息。

---

## 1. 功能模块

| 模块 | 说明 |
|------|------|
| 设备台账 | 维护设备编号、名称、型号、所属点检模板与所在房间。 |
| 点检模板 | 定义点检项目（内容 + 频次），支持新增 / 编辑 / 复制 / 导入。 |
| 点检作业 | 按设备每日点检，记录状态（正常 / 异常）、异常说明，并完成电子签名。 |
| 状态大屏 | 按设备 / 任务铺卡片，直观展示当日点检完成情况。 |
| 月度点检表 | 按房间 / 设备生成月度点检汇总，支持打印视图。 |
| 温湿度监测记录 | 房间温湿度填录、一键填充、划线；报警配置与告警查看；每房间可设**专属范围**。 |
| **数据定时备份** | 把全量数据定时自动落盘到本机文件夹，多目标互为副本、逐目标频率、按 `年/月` 分层归档，并配套落一份含密钥的配置；后台可视化状态 / 历史 / 文件清单。 |
| 独立检查项目 | 与常规点检分离的专项检查表（programs），如「摩擦和风阻损耗检查」。 |
| 点检记录 | 独立项目的按日期 / 周次记录（checkRecords）。 |
| 通知 | 系统内通知（notifs）。 |
| 权限管理 | 细粒度权限：点检模板管理、温湿度录入 / 填充 / 删除、批量点检、导出等。 |
| LIMS 对接 | 保留接口结构，地址 / 账号 / 密码均为占位符，默认关闭；湿度抓取按 ≤20 天分段串行拉取（LIMS 单次跨度上限 23 天）。 |
| 局域网远程升级 | 保留接口结构，默认关闭（演示 / 开源版不含升级包）。 |

---

## 2. 数据模型（KV 顶层键）

| 键 | 类型 | 说明 |
|----|------|------|
| `admin` | null / object | 兼容旧版管理员对象（新版本由 `users` 承担）。 |
| `users` | array | 账号：`id / username / name / password_hash / role / active / created_at / dept / perms`。 |
| `devices` | array | 设备：`id / no / name / model / template_id / location`。 |
| `templates` | array | 点检模板：`id / key / equip_name / model / source_file / items[] / note`。 |
| `signers` | array | 签名人：`id / name / active / password_hash / signature_image / sig_chars / sig_dir`。 |
| `inspections` | array | 点检记录：`device_id / inspect_date / status / signed_by / signer_id / signature_image / abnormal_note / bySn / signed_at`。 |
| `abnormalRecords` | array | 异常记录。 |
| `rooms` | array | 房间：`name / desc / thermo_apparatus / thermo_requirement / thermo_equipment / group / dept / form_head`。 |
| `depts` | array<string> | 科室清单。 |
| `envRecords` | array | 温湿度记录：`id / room / ym / cells{slot:{temp,humidity,recorder,signature_image,strike}}`。 |
| `programs` | array | 独立检查项目：`id / device_ids / name / title / form_code / columns[] / criteria[] / side_items[] / ...`。 |
| `checkRecords` | array | 独立项目记录。 |
| `formHeads` | object | 表单页眉四档配置（`env / device / program / checkall`，各含 `code / rev / text`）。 |
| `links` | object | 顶栏外部系统跳转：`items:[{key,name,url,depts,enabled}]` 与 `manual:{enabled}`。 |
| `backup` | object | **数据定时备份**配置：`enabled / targets[] / freq / weekday / day / hours / time / keep / retry / compress / history[] / lastRun / lastAuto{}`。 |
| `lims` | object | LIMS 对接配置（占位符，默认关闭）。 |
| `envfill` | object | 温湿度录入时间范围等。 |
| `signNoPw` | boolean | 签名是否免密。 |
| `lanClients` | array | 局域网客户端（开源版为空）。 |
| `_archivedTemplates` | array | 归档模板。 |

### 2.1 `backup` 子结构（v1.41.0 新增）

```jsonc
{
  "enabled": false,              // 总开关；关闭后谁都不自动备份（「立即备份一次」仍可用）
  "targets": [                   // 多目标：每项 = 一个目录 + 它**自己**的一套策略
    { "dir": "",                 //   dir 空串 = 程序目录下的 backups/
      "enabled": true,           //   该目标是否参与定时（关掉后定时与「立即备份」都不写它）
      "freq": "weekly",          //   weekly | daily | hourly | monthly
      "weekday": 0,              //   freq=weekly 时生效：0=周日 … 6=周六
      "day": 1,                  //   freq=monthly 时生效：每月第几号（当月无此日号 → 顺延当月最后一天）
      "hours": 6,                //   freq=hourly 时生效：每几小时（1~23，按 0 点起算的整点间隔）
      "time": "19:00",           //   daily/weekly/monthly = 执行时刻；hourly = 每个间隔内的第几分钟
      "keep": 12 }               //   0 = 全部保留；>0 = 只保留最近 N 份（连同配套 .config.json 一起清）
  ],
  "freq": "weekly", "weekday": 0, "day": 1, "hours": 6, "time": "19:00", "keep": 0,
                                 // 顶层同名老字段 = 「新建目标时继承的默认策略」；
                                 // 本次未传 targets 时也会批量应用到所有目标（兼容老调用方）
  "retry": 3,                    // 全局：最多尝试次数（含首次），重试退避 3s/6s/9s
  "compress": false,             // 全局：true = gzip 后再落盘
  "history": [...],              // 最近 50 次执行记录（含逐目标落盘情况）
  "lastRun": {...},              // 最近一次执行的结果快照
  "lastAuto": {                  // 逐目标判重台账，键 = 目标目录绝对路径小写
    "<目标key>": { "periodKey": "2026-W39", "at": "...", "ok": true }
  }
}
```

**为什么「何时留一份、留几份」逐目标、「怎么写这一份」全局统一**：多目标共用同一份序列化字节（这是「各副本逐字节一致」的基础）。若各目标的压缩方式不同，就得为每个目标单独生成一份字节，这个性质就没了。

**周期键（`periodKey`）**：判重粒度随频率变化，且**落盘**（进程重启 / 升级后不会把同一周期再跑一遍）。

| 频率 | 周期键示例 |
|------|-----------|
| `weekly` | `2026-W39`（ISO 周，周一为一周之始） |
| `daily` | `2026-09-23` |
| `hourly` | `2026-09-23H18`（按 N 小时分桶） |
| `monthly` | `2026-09` |

**其他口径**：触发窗口 5 分钟（tick 每 30 秒一次）；**只有「自动备份成功」才封周期**（手动备份不参与判重，否则周一随手手动备一次，本周日的自动备份就被跳过了）；落盘用「先写 `.tmp` 再 `rename`」；写前检查磁盘余量；路径无效 / 无权限 / 空间不足属**不可重试**错误，直接失败并写运行日志（标签 `BACKUP`）。

### 2.2 字段类型契约（⚠️ 自行扩充数据时最容易踩）

这几个字段「类型对了但取值不对」时，**页面不会报错、控制台干净，只是渲染出 `undefined` / `[object Object]` / 「—」**。

| 字段 | 必须是 | 常见错误 | 表现 |
|------|--------|----------|------|
| `checkRecords[].rows[日].<conclusion列key>` | 内部码 `normal` / `abnormal` / `slash` | 直接写显示字符 `√` / `×` / `/` | 前端查 `CONC_MARK[v]` 落空 → 渲染出字面量 **`undefined`** |
| `programs[].side_items` | **字符串数组** `["检查砧座紧固", …]` | 写成对象数组 `[{sn:1,content:"…"}]` | 前端 `map(x => esc(x))` → 渲染成 **`[object Object]`** |
| `signers[].sig_chars` | **数组**（签名笔画图，可为空 `[]`） | 写成字符串（如 `"演示"`） | 后台「字符数」读成字符串长度 |
| `signers[].sig_dir` | `v` / `h` / `auto` | 写成 `right` / `center` | 被规范化成 `auto`（静默失真） |
| `envRecords[].updated_at` | ISO 时间字符串 | 缺失 | 温湿度页恒显示「**尚未保存**」 |
| `envRecords[].cells` | `{ "8_PM": {temp,humidity,recorder,signature_image,strike} }` | 键写成 `8-PM` / `8pm` | 该格不参与渲染，等于丢数据 |
| `templates[].items` / `programs[].criteria` / `columns` | 数组（元素为对象） | 写成对象 | 检查表整页空白 |

**还有三类「引用完整性」问题**同样不报错、只是显示不出来：`devices[].template_id` / `devices[].location` / `inspections[].signer_id` 指向的 id 必须在对应集合里存在。

> 本仓库带一个深度审计脚本可一次查完全部上述问题（类型 + 枚举 + 引用完整性 + PII 兜底）。

---

## 3. 接口契约（节选 · 读取类）
> 演示站（Cloudflare Pages）暴露**读取类**接口并拦截一切写入；其中「登录」是唯一的写方法例外。
> 自托管版本（Node / Windows 版）具备完整读写接口。

### 3.1 只读策略（演示站）

| 请求类型 | 演示站行为 |
|----------|-----------|
| 读取类 GET（下表全部） | ✅ 直接返回数据，无需登录 |
| `POST /api/admin/login`（登录） | ✅ 允许（只做口令校验 + 签发 cookie，不写任何数据） |
| 其它 POST / PUT / DELETE | ❌ `403 {readonly: true, ...}` |
| `GET /api/admin/backup`（整库导出，含 `pepper` / `secret` / 口令哈希） | 🔒 需有效登录会话，匿名返回 `401` |

写入被拒时的响应体：

```json
{ "readonly": true, "error": "演示站为只读展示模式，禁止任何写入 / 新增 / 编辑 / 删除操作" }
```

### 3.2 读取类接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/version` | 版本号与只读标记（`readonly` / `demo`）。 |
| GET | `/api/admin/me` | 当前登录用户（未登录返回 `ok:false`）。 |
| GET | `/api/devices` | 设备列表。 |
| GET | `/api/inspect/device/:id` | 单台设备的点检明细（含检查项与签名）。 |
| GET | `/api/templates` | 点检模板列表。 |
| GET | `/api/inspections` | 点检记录。筛选维度：`?device_id=`（设备编号或 id）、`?date=`、`?name=`（**同时**匹配签名人姓名与设备名，`?signer=` 为兼容别名）、`?room=`（按设备所在房间）。响应每条含 `id/inspect_date/no/name/room/status/signed_by/abnormal_note` —— 其中 **`room` 就是「房间」列的取值来源**，缺失时整列显示「—」。 |
| GET | `/api/monthly` | 月度点检汇总（**需 `?month=YYYY-MM`**，缺失返回 400）：`{month, devices[], sig_assets{}, form_head_checkall}`，每台设备带 `items/days/signers/abnormal/form_code/form_rev/form_head/title/tpl_key`。 |
| GET | `/api/dashboard` | 大屏状态数据。筛选维度：`?date=`（默认今日）、`?location=`（单房间）、`?group=`（**整个分组**，与 `location` 互斥且优先）。响应：`{total,today_inspected,today_abnormal,month_inspected,grid[],recent[],job_total,job_done,job_abnormal,jobs[]}` —— 大屏 KPI 卡读的是 **`job_*` 四个字段**（`jobs` = 设备点检任务 + 专项检查任务，一台设备可能同时有两者，故任务数 ≥ 设备数）。 |
| GET | `/api/rooms` / `/api/depts` | 房间（含 `form_head` 房间级页眉覆盖、`env_range` 房间级专属温湿度范围、`group` 分组、`dept` 科室）/ 科室。 |
| GET | `/api/users` / `/api/signers` | 用户 / 签名人（均**不含**密码哈希）。 |
| GET | `/api/signers/public` | 公开签名人（仅姓名，供前端选择）。 |
| GET | `/api/signers/:id/sig` | 单个签名人的签名图。 |
| GET | `/api/programs` | 独立检查项目（附带 `device_count` / `rooms` / `devices` / `form_head`）。 |
| GET | `/api/form-heads` | 表单页眉四档（`{items:[{kind,name,hint,code,rev,text,line,cfg_code,cfg_rev,def_code,def_rev,carrier_driven}], env_room_map{}}`）；写方法 `PUT /api/form-heads`、`POST /api/form-heads/reset` 在演示站返回 403。 |
| GET | `/api/check-records` | 检查记录（支持 `?program_id=&device_id=&ym=`）。 |
| GET | `/api/env-records` | 温湿度记录（**需 `?room=&ym=`**）：`{id,room,ym,cells{},updated_at}`。`updated_at` 是温湿度页「上次保存：…」的来源，缺失时会一直显示「尚未保存」。 |
| GET | `/api/env/alerts` | 温湿度超限提醒（`{ok,dept,maxLevel,counts,alerts}`，按近两月逐格比对房间要求）。 |
| GET | `/api/notifs` | 站内通知（`{ok,unread,rows}`）。 |
| GET | `/api/settings` | 设置：`{signNoPw, links:{items:[{key,name,url,depts,enabled}], manual:{enabled}}}` —— 顶栏外部系统跳转与操作手册入口开关。 |
| GET | `/api/env-alerts` | 温湿度预警记录列表（`{ok,rows}`，支持 `?status=&room=`）。 |
| GET | **`/api/backup/config`** | **数据定时备份状态**（v1.41.0）：`{cfg, targets[], default_policy, default_dir, max_dirs, summary, history[], last_run, next_run_local, weekdays[], freqs[], freq_desc, counts, app_version}`。`targets[]` 每项带它自己的策略 + 实时状态 + 下次执行：`{raw,dir,key,enabled,freq,weekday,day,hours,time,keep,freq_desc,next_run_local,last_auto,period_key_now,last_auto_current,writable,write_error,disk_free,disk_free_text}`。写方法 `PUT /api/backup/config`、`POST /api/backup/run`、`POST /api/backup/test` 在演示站返回 403。 |
| GET | **`/api/backup/files`** | **已生成的备份文件清单**（v1.41.0）：`{ok, targets:[{label,dir,error,count}], total, total_size_text, files:[{rel,size,size_text,mtime,mtime_local,dir_abs,dir_key}]}`（按目标分组 + 每组扫到几份，与 `history` 互为印证）。 |
| GET | `/api/lims/config` / `/api/lims/room-status` | LIMS 配置（占位符，无真实凭据）/ 房间同步状态。 |
| GET | `/api/lims/job/:id` | LIMS 抓取任务进度（演示站无任务，忠实返回 404）。 |
| GET | `/api/lan/clients` / `/api/lan/packages` / `/api/lan/scan` / `/api/lan-upgrade/config` / `/api/lan-upgrade/ping` | 局域网与远程升级（演示站返回安全空数据 / 不可升级）。 |
| GET | `/api/admin/logs` | 运行日志（演示站返回空）。 |

> ⚠️ **演示站上「数据备份」接口的语义边界**：Serverless 运行环境（Cloudflare Workers）**没有文件系统**，
> 因此这两个接口**不做任何伪造** —— 目标文件夹的可写状态与磁盘余量一律如实报「不可用」；
> 「已生成的备份文件」由演示数据里的 `history` 推导（该接口本来的设计意图就是「与历史记录相互印证」）。
> 响应里带 `readonly: true` 与 `demo_note` 说明这一点。完整能力请在自托管版本上体验。

---

## 4. 部署形态

- **Cloudflare Pages 演示站**：静态前端 + `functions/[[path]].js` 只读后端，数据来自 KV。
- **Node 服务版**：`node server.js`，数据落地 `data/kv.json`。
- **Windows 便携版**：同 Node 版，附带便携运行时与 `启动.bat`。

## 5. 操作手册与静态页面

| 路径 | 说明 |
|------|------|
| `/manual`（=`/manual.html`） | **开源版为中性说明页**：内置手册含 18 张真实界面截图，无法文本脱敏，故未纳入开源版。自托管版本可自行放入完整手册，路径与入口不变。 |
| `/v3/v3-preview.html` | UI 改版提案预览页（仅供设计参考，已脱敏）。 |

> 顶栏「📖 操作手册」入口是否显示由 `kv.links.manual.enabled` 控制（后台「系统 → 外部系统跳转」面板）。
